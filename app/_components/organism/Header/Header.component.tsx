import { Navbar } from "@app/_components/molecules";
import React from "react";

export interface HeaderProps {}

export const Header = (props: HeaderProps) => {
  return <Navbar />;
};

export default Header;

